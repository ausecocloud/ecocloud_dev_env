# This is a docker-compose feature.
# docker will not read this file. Only docker-compose will read it.

# this is necessary here, because jupyterhub will need to know the full network name
# generated by docker-compose. Make sure to always set this env var, or otherwise
# the ntework name for jupyterhub dockerspawner will be wrong.
COMPOSE_PROJECT_NAME=ecocloud_dev
